// cara menampilkan array

let hewan = ["Sapi", "Kambing", "Kucing"];

// for in untuk menampilkan indeks dari sebuah array
for (let index in hewan) {
    console.log(index);
}

// for of untuk menampilkan element dari sebuah array
for (let element of hewan) {
    console.log(element);
}
